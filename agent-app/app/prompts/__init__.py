"""Externalized LLM prompts, organized by locale then node.

Prompts are loaded via app.prompts.loader (load_system, render_human, load_text)
instead of being hardcoded as Python string constants in node modules.
"""
